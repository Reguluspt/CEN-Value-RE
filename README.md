# CEN Value RE

Repository baseline for the CenValue RE project.

The first complete project import is proposed through a pull request so the imported workspace can be reviewed before it becomes the `main` baseline.
