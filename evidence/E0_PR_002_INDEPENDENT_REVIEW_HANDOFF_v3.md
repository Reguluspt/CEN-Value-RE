# E0-PR-002 — Independent Re-review Handoff v3

**Date:** 2026-08-16  
**Repository:** `Reguluspt/CEN-Value-RE`  
**PR:** `#3`  
**Branch:** `agent/e0-pr-002-astryx`  
**Implementation baseline:** `94ff266a3686b5b5bfd98cb55459dbe7a6cf24d8`  
**Prior reviewed HEAD that returned findings:** `96feb62572f6668c91b453f2a501569fbe9ed4f4`  
**Corrective tested input HEAD:** `bbc7b3dea0bed562124a320149722623e3d72ca4`  
**Successful corrective run:** `31951150586` attempt 2  
**Decision requested:** CLOSE / KEEP OPEN for each finding, then ACCEPTED / RETURN FINDINGS

## Exact review-head rule
Before re-review, resolve the **current exact PR HEAD from GitHub** and compare it to corrective tested input `bbc7b3dea0bed562124a320149722623e3d72ca4`.

After the successful corrective run, any delta must be limited to:
- committed v3 evidence generated by the successful workflow;
- removal of the one-time findings-verification workflow;
- evidence/report/re-review-handoff documentation only.

If a tested implementation file changed after `bbc7b3dea0bed562124a320149722623e3d72ca4`, do not accept without new runtime evidence.

## Findings to re-review

### E0-PR-002-F001 — HIGH
Prior issue: raw Astryx vendor CSS introduced persistent global `:root` / `html[data-theme]` token declarations into the document root.

Corrective implementation:
- raw vendor CSS is no longer directly imported by authored RE CSS;
- `web/scripts/generate-re-astryx-css.mjs` resolves exact pinned vendor CSS before standard `dev`, `build`, and static verification;
- `web/scripts/scope-re-astryx-css.mjs` rewrites the known pinned vendor global token selectors onto `.cenvalue-re-surface` and fails closed on vendor-selector contract drift;
- generated scoped CSS is ignored by Git and imported by `ReShell`;
- Astryx root `Theme` provider is not mounted because v0.2.0 synchronizes theme attributes to `document.documentElement`;
- `data-theme` and `data-astryx-theme="neutral"` are local to `.cenvalue-re-surface`.

Closure evidence:
- `evidence/E0-PR-002_built_css_verification_v3.log`: emitted RE CSS contains no `:root`, `html[data-theme]`, or global `body` selector while retaining Astryx token declarations;
- `evidence/E0-PR-002_browser_smoke_v3.log`: in both light and dark environments, documentElement attributes and **every computed root CSS custom property** remain unchanged after a client-side `/re` visit; representative `/dashboard` and `/cases` styles also remain unchanged.

### E0-PR-002-F002 — MEDIUM
Prior issue: verifier and browser harness could not detect F001.

Corrective implementation:
- static verifier resolves the exact raw vendor CSS packages, confirms the known global-selector mutation exists as a negative control, and verifies deterministic scoped output;
- a second verifier scans the emitted `ReShell-*.css` build artifact for forbidden global selectors;
- browser snapshot captures root attributes, every computed root custom property, root/body computed styles, and representative legacy control styles;
- browser runs the normal acceptance case in both light and dark environments;
- no warning/selector text whitelist is used for root mutation detection.

Closure evidence:
- `evidence/E0-PR-002_static_negative_control_v3.log`: synthetic `:root { --border-width; --color-accent }` CSS is rejected by the built-CSS verifier;
- `evidence/E0-PR-002_browser_negative_control_v3.log`: the same reviewer-observed root mutation is detected as a before/after root-custom-property difference and causes the browser gate to fail;
- `evidence/E0-PR-002_static_verification_runtime_v3.log`: normal static verification passes after deterministic containment;
- `evidence/E0-PR-002_browser_smoke_v3.log`: corrected normal browser case passes in light and dark.

## Complete v3 runtime result
GitHub Actions run `31951150586` attempt 2, Node `22.13.0`:
- scoped CSS generation: PASS;
- static verifier: PASS;
- scoped lint: PASS;
- full legacy lint non-regression: PASS (`88` baseline / `88` after / `0` unchanged-file regressions);
- production build: PASS;
- emitted built CSS containment: PASS;
- static root-leak negative control: REJECTED as expected;
- browser root-leak negative control: REJECTED as expected;
- unauthenticated `/re` -> `/login`: PASS;
- guest `/re` -> `/sobo`: PASS;
- admin `/re` + two Astryx TextInputs: PASS in light and dark;
- document-root custom-property/attribute isolation: PASS in light and dark;
- legacy `/dashboard` / `/cases` style non-regression: PASS in light and dark;
- new page errors: `0`;
- new console errors: `0`.

Primary evidence: `evidence/E0_PR_002_RUNTIME_EVIDENCE_v3.md`.

## Required reviewer output
For each finding explicitly state:
- `E0-PR-002-F001: CLOSED` or `OPEN` with reason;
- `E0-PR-002-F002: CLOSED` or `OPEN` with reason.

Then return exactly one overall verdict:
- `ACCEPTED`, or
- `RETURN FINDINGS` with remaining finding(s).

E0-PR-002 remains **not self-accepted**. PR #3 must not merge and E0-PR-003 must not start until the independent reviewer returns `ACCEPTED` against the exact current review HEAD.
