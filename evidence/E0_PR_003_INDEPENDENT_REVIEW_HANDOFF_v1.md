# E0-PR-003 — Independent Review Handoff v1

**Date:** 2026-08-16
**Repository:** `Reguluspt/CEN-Value-RE`
**Branch:** `agent/e0-pr-003-decimal-rounding`
**Implementation baseline:** `d89334c4c0ba5a666d4ce5556bc665d6e74750c0`
**Runtime-tested HEAD:** `6e966d174efe0fb3072d20167ebdf636de1c4529`
**Successful runtime run:** `31953123525`
**Focused result:** `44 passed in 0.09s`
**Decision requested:** ACCEPTED / RETURN FINDINGS

## Exact review-head rule

Before issuing a verdict, resolve the current exact PR HEAD from GitHub. Compare runtime-tested HEAD `6e966d174efe0fb3072d20167ebdf636de1c4529` to the current review HEAD.

Any post-test delta must be limited to:

- runtime evidence generated by the successful workflow;
- removal of the one-time verification workflow;
- implementation report / independent-review handoff documentation.

If an implementation or test file changed after the runtime-tested HEAD without a corresponding re-run, do not accept.

## Frozen authority

Review against the current Epic 0 / Gate B contracts, especially:

- `epic-0/EPIC_0_PR_PLAN_v1.md` — E0-PR-003 scope and N08 acceptance vectors;
- `gate-b/GATE_B13_ROUNDING_POLICY_v1.md` — increments, policy resolution and audit metadata;
- `gate-b/GATE_B4_ROUNDING_RECALCULATION_STRATEGY_v0.1.md` — Decimal/no-binary-float rule and explicit checkpoint rounding;
- current Design Book/session corrective — `5% = Decimal("0.05")` and separate before-rounding/final-rounded valuation concepts.

Do not redesign or expand into E0-PR-004/ExcelTemplateProfile or Epic 1 valuation formulas.

## Expected implementation surface

Implementation:

- `src/re/domain/common/__init__.py`
- `src/re/domain/common/numeric.py`
- `src/re/domain/common/rounding.py`

Tests:

- `tests/re/test_numeric_primitives.py`
- `tests/re/test_rounding_policy.py`

Evidence/report files under `evidence/` and `implementation/` are expected.

## Required technical review

### 1. Decimal primitives

Confirm:

- Money, Percentage and UnitPrice are Decimal-backed and framework-independent;
- Percentage canonical representation is fractional (`5% = 0.05`), not `5`;
- canonical primitives do not pass through binary `float`;
- invalid/non-finite values fail safely;
- construction of value objects does not introduce implicit rounding.

### 2. RoundingPolicy contract

Confirm:

- `NONE` is represented distinctly;
- explicit increments require positive whole VND;
- 1k / 10k / 100k / 1m / 10m and custom increments work;
- only frozen `NEAREST` mode is implemented;
- midpoint behavior matches Excel nearest rounding for positive and negative values;
- raw values are retained separately from rounded values;
- rounding remains deterministic even if external Decimal context precision changes.

### 3. Resolver / provenance

Confirm resolution precedence exactly:

`case override -> template/profile default -> application default`.

Confirm:

- explicit case-level `NONE` is not mistaken for missing override;
- wrong target/source candidates fail closed;
- case overrides require `selected_by` and `selected_at`;
- profile metadata can be carried without coupling to Excel implementation;
- target identifiers remain extensible for future template targets without changing the calculation core.

### 4. Frozen N08 vectors

Confirm exact tests:

- `196,308,350` at UNIT_PRICE 1,000 -> `196,308,000`;
- `19,581,412,440` at TOTAL_VALUE 1,000,000 -> `19,581,000,000`.

These are template-default examples, not hard-coded application-global rules.

### 5. Architecture / scope

Confirm no:

- valuation/calculation formula engine beyond rounding primitive;
- Excel adapter/profile implementation;
- persistence/database implementation;
- API/UI/provider implementation;
- Epic 1 feature creep;
- framework/infrastructure dependency entering Domain.

### 6. Evidence

Primary evidence:

- `evidence/E0_PR_003_RUNTIME_EVIDENCE_v1.md`;
- `evidence/E0-PR-003_tests_v1.log`.

Run `31953123525` is expected to establish Python 3.11.15 and **44 passed in 0.09s**, including the existing architecture/import regressions plus the new E0-PR-003 tests.

The prior failed workflow attempt was a temporary CI-file trailing-whitespace failure before pytest and is not acceptance evidence.

## Finding format

If a finding exists, report:

- ID;
- severity: BLOCKER / HIGH / MEDIUM / LOW;
- file/path and exact issue;
- why it matters;
- required corrective action;
- acceptance test for closure.

Do not raise cosmetic/style preferences as blocking findings.

## Required output

Return:

```text
E0-PR-003 INDEPENDENT REVIEW

Repository:
PR:
Base:
Runtime-tested HEAD:
Reviewed HEAD:
Runtime run:

1. Scope Review
...

2. Decimal Primitive Review
...

3. RoundingPolicy Review
...

4. Resolver / Provenance Review
...

5. Runtime / Evidence Review
...

6. Evidence Binding Review
...

FINDINGS
- NONE

VERDICT:
ACCEPTED

E0-PR-003 may proceed to merge and E0-PR-004 may begin after merge.
```

or `RETURN FINDINGS` with the open findings.

E0-PR-003 remains **not self-accepted** until this independent verdict is supplied and bound to the exact reviewed PR head.
