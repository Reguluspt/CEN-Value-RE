# 11 — Historical Learning
**Status: DRAFT**

User filters historical files first. Pipeline:
`Template Fingerprint → Deterministic Extractor → Normalized Case → AdjustmentObservation → Statistical Patterns → optional AI semantic analysis`.
Deterministic before AI.

Preserve provenance and distinguish explicit zero from missing. Case-level inclusion flag. After approval, use supported field-level approved decisions as preferred targets while retaining suggested/appraiser/approved lineage.

**OPEN:** similarity, template-family clustering, quality gates, statistics/confidence, dataset version/snapshot, AI boundary.
