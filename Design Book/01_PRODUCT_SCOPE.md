# 01 — Product Scope
**Status: FROZEN**

GĐ1 closed loop:
`Create/manage case → TSTĐ → TSSS → CTXD → Adjustment → Valuation Result → Excel approval export → returned approval import → close case`.

Mandatory: Case Portfolio/autosave/resume; canonical TSTĐ; GCN staging/human review; Maps→lat/lng; multiple CTXD; TSSS Quick/Expanded Entry; Adjustment Engine + historical suggestions; comparable quality + human decision; Excel compatibility; Approval Round-trip.

GĐ2: property/TSSS knowledge stores, identity, price history, comparable search, geospatial/property knowledge. GĐ1 must prepare structured data without destructive redesign.

Deferred: enterprise warehouse, advanced GIS, workflow automation, revenue/CRM, mobile/web, multi-user, internal OCR server, full Company Adjustment Rule Engine runtime.
