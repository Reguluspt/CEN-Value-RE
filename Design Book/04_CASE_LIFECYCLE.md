# 04 — Case Lifecycle
**Status: FROZEN**

`DRAFT → IN_PROGRESS → READY_FOR_APPROVAL → EXPORTED_FOR_APPROVAL → APPROVAL_RETURNED → REVISION_REQUIRED | APPROVED → CLOSED`.

Manage multiple concurrent cases. Autosave by default. `CaseWorkspaceState` remembers last section/comparable/CTXD/focused cell/grid/scroll/panels.

Case-level option controls Historical Learning inclusion; no exclusion reason required.
